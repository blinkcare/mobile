import React from 'react'
import { View } from 'react-native'
import styled from 'styled-components/native'
import { space, width, color, borderWidth, borderColor } from 'styled-system'

const Box = styled.View([], space, width, color, borderWidth, borderColor)

export default Box

import React from 'react'
import { View } from 'react-native'
import styled from 'styled-components/native'
import { space, width, borderWidth, borderColor } from 'styled-system'

const Box = styled.View([], space, width, borderWidth, borderColor)

export default Box
